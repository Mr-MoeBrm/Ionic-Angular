import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.angular.ionic',
  appName: 'Ionic-Angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
